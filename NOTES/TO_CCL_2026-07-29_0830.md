# TO CCL — 2026-07-29 08:30 — polval documentation pass (doc-only)

**Repo/branch:** `macos` on `pol-core`.  Nothing in `MACOS_resources`
changed.  No engine change, no measurement, no test-suite edit, no fixture
moved.  `generated/numbers.json` is untouched — the report was **re-rendered**
(stage 3, `render_polval.py`), not re-measured.

One bundled doc item, two halves, both Fable-ruled in
`REVIEW_POL_OVERCOAT_CHROMATIC_2026-07-28.md`.

---

## A — the reader's digest

`docs/macos-manual/polval/05_summary.md.in` (new).  Sorts between
`00_frontmatter` and `10_conventions`, so it renders as an **unnumbered**
front section after the frontmatter and before §1.  Motivation: the primary
reviewer will not read 47 pages.

Contents: scope in one paragraph; the conventions table **verbatim** (review
priority 1); one short paragraph per major result, each anchored by its key
number — r_p sign (Py/Px `1.0163e+00` → `2.0724e-04`, ρ² slope `1.871`),
coated-transmission radiometry (`0.6666666667` = 1/1.5 pre-fix),
polarizer/waveplate + material-axis rule (`3.56°` at 20° AOI), the van Harten
anchor (`2.828e-14` / `4.937e-14` vs ±`0.01`), the overcoat reversal
(`25.90×`, both sides engine-measured), the vector chain (`6.42e-16` worst
case on the exact class), and the contrast floor with the Tranche-1 boundary
stated **as a boundary** in a pull-quote rather than buried (carried
`0.8412` bare / `0.5653` coated, and the grid gets the coating **sign**
wrong); then a where-to-go-deeper table (section → packet).

**Token reuse only.**  Every number is an existing `@@TOKEN@@` from the
current `numbers.json` / `external.json`.  No new tokens, no hand-typed
values, so `check_polval` needed no change to stay green.

**Standalone hand-out:** `make polval-summary` →
`build/macosPolVal_summary.{pdf,docx,html}` from the same single source.
No TOC on a short document.  **It renders to 4 pages, not the 3 asked for**:
three of prose plus the packet-pointer table.  The verbatim conventions
table (with its provenance column) is ~1 page on its own; getting to 3 would
have meant cutting either that table or result substance, and I judged
neither worth it.  Say the word and I will drop the table's third column —
that alone gets it to 3.

**Settled 2026-07-30 — Dave: "4 pages is good."**  The conventions table
keeps its provenance column and the summary stays at 4 pages.  Do not
re-open this to chase a page count.

**Drift guard (small addition to `tools/check_polval.py`).**  The conventions
table now exists in two files, which is exactly the arrangement Fable flagged
as drift-prone for §5.5.  The table is bracketed by
`<!-- CONVENTIONS-TABLE:BEGIN/END -->` in both, and `check_polval` now
compares every same-tagged block byte for byte.  Verified non-vacuous: a
one-word edit to the summary's copy makes the guard fail with a named
message.  This is the only behavioural change to the tooling.

## B — the §5.5 shrink

`polval/60_contrast_floor.md.in`.  Per the ruling, the correction *notice*
stays (a published number was mislabeled; that trail is not erasable) but
reduces to three things: **what was wrong**, the **chromatic design rule**,
and a **pointer to §8.3.1 as the single home of the physics**.  Removed: the
restated mechanism (runtime `set_src_wvl`, physical-vs-waves thickness) and
the duplicated magnitudes — the reversal ratio, both wavelength legs, the
true-quarter-wave ratio, and the excess-ratio pair.  All of those still live
in §8.3.1, unchanged; §5.5's own measured `151.3×` is unchanged and stated
as still correct for the film as configured.

Also fixed in the same paragraph block: §5.3's cross-reference to the
grid-carry discussion said "(§5.5)"; it is §5.6.  One character, same file,
verified against the section it points at.

---

## Verification

| check | result |
|---|---|
| `render_polval.py` (stage 3 only — no MATLAB, no re-measure) | 12 sections written |
| `make polval-check` | green, 12 sections consistent with `numbers.json` |
| unresolved `@@TOKEN@@` in any rendered `.md` | none |
| **numbered-heading diff, before vs after** | **identical — 43 numbered headings, byte-for-byte** |
| full heading diff | only the summary's 5 new *unnumbered* headings |
| `make polval` / `polval-pdf` | docx + HTML + 47-page PDF |
| `make polval-summary` | 4-page standalone PDF + docx + HTML |
| new drift guard, against a deliberately drifted copy | fails with a named message |

**Nothing else was re-run, and nothing else can have moved:** this slice
touches only Markdown prose, one Makefile, and one consistency script.  The
engine, the fixtures, the MATLAB tools and both test suites are bit-for-bit
as they were at `704e1ca`.

## Open / next

- ~~Page count 4 vs 3~~ — **settled 2026-07-30, Dave: 4 pages is good.**
  Conventions table keeps its provenance column.  Nothing to do.
- Nothing else queued from the overcoat verdict; that worklist is closed.
- **This slice is closed.**
